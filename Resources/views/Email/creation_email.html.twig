{% extends "email.html.twig" %}

{% block subject %}
{{ 'email.creation.title'|trans({}, 'EkynaUser') }}
{% endblock subject %}

{% block body %}
{{ 'email.creation.message'|trans({
    '%sitename%': sitename,
    '%login_url%': login_url,
    '%login%': login,
    '%password%': password
}, 'EkynaUser')|raw }}
{% endblock body %}
