{% extends "EkynaAdminBundle:Entity:sided.html.twig" %}

{% block title %}
<h1>{{ 'ekyna_user.group.header.remove'|trans({'%name%': group.name})|raw }}</h1>
{% endblock %}

{% block actions %}
{{ admin_resource_btn(group, 'list') }}
{{ admin_resource_btn(group, 'new') }}
{% endblock %}

{% block main %}
<form action="{{ path('ekyna_user_group_admin_remove', identifiers|merge({'groupId': group.id})) }}" method="post" class="form-horizontal">
    <div class="form-body">
	    {{ bootstrap_set_style('horizontal') }}
		{{ form_widget(form) }}
    </div>
    {{ ui_form_footer(form) }}
</form>
{% endblock %}

{% block side %}
{% endblock %}